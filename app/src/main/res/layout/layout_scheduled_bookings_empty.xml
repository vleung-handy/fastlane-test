<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout
    android:id="@+id/scheduled_bookings_empty"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:gravity="center"
        android:orientation="vertical"
        >

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_no_scheduled_jobs"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/default_margin_large"
            android:layout_marginLeft="@dimen/default_margin_xlarge"
            android:layout_marginRight="@dimen/default_margin_xlarge"
            android:layout_marginTop="@dimen/default_margin"
            android:gravity="center_horizontal"
            android:text="@string/no_scheduled_jobs"
            android:textColor="@color/subtitle_grey"
            android:textSize="@dimen/large_text_size"
            />

        <Button
            android:id="@+id/find_jobs_for_day_button"
            style="@style/Button.PartnerBlueEmpty.Round"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="@string/cta_find_jobs_for_this_day"
            />

    </LinearLayout>
</android.support.v4.widget.SwipeRefreshLayout>
