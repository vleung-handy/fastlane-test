<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              tools:context=".ScheduledBookingsFragment">

    <TextView
        android:id="@+id/scheduled_bookings_banner_text"
        style="@style/Banner"
        android:text="@string/scheduled_jobs"/>

    <include layout="@layout/layout_fetch_error"/>

    <LinearLayout
        android:id="@+id/bookings_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="gone">

        <HorizontalScrollView
            android:id="@+id/scheduled_bookings_dates_scroll_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/scheduled_bookings_dates_scroll_view_layout"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal">
            </LinearLayout>

        </HorizontalScrollView>

        <include layout="@layout/layout_scheduled_bookings_empty"/>

        <com.handy.portal.ui.element.BookingListView
            android:id="@+id/scheduled_jobs_list_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="1dp"
            android:divider="@color/white_trans"
            android:dividerHeight="1dp"/>

    </LinearLayout>

</LinearLayout>
