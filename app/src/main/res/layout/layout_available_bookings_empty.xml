<?xml version="1.0" encoding="utf-8"?>
<com.handy.portal.library.ui.widget.SafeSwipeRefreshLayout
    android:id="@+id/available_bookings_empty"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:visibility="gone"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:gravity="center"
        android:orientation="vertical"
        >

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_no_available_jobs"
            />

        <TextView
            android:id="@+id/available_bookings_empty_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/default_margin_xlarge"
            android:layout_marginLeft="@dimen/default_margin_xlarge"
            android:layout_marginRight="@dimen/default_margin_xlarge"
            android:layout_marginTop="@dimen/default_margin"
            android:gravity="center_horizontal"
            android:text="@string/no_available_jobs"
            android:textColor="@color/tertiary_gray"
            android:textSize="@dimen/large_text_size"
            />

        <Button
            android:id="@+id/available_bookings_see_all_jobs_button"
            android:layout_width="130dp"
            android:layout_height="40dp"
            android:background="@drawable/button_blue_borders"
            android:text="@string/job_all"
            android:textColor="@color/handy_blue_text"
            android:gravity="center"
            android:textAllCaps="false"
            style="@style/TextView.Medium"
            android:visibility="gone"/>
    </LinearLayout>
</com.handy.portal.library.ui.widget.SafeSwipeRefreshLayout>
