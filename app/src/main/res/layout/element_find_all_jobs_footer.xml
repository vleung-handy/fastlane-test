<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="wrap_content">
    <Button
        android:id="@+id/available_jobs_find_all_button"
        style="@style/TextView.Medium.Bold"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="6dp"
        android:background="@drawable/border_gray_bg_white"
        android:stateListAnimator="@null"
        android:gravity="center"
        android:text="@string/available_job_all"
        android:textAllCaps="false"
        android:textColor="@color/handy_blue_text"
        android:visibility="gone"
        tools:text="See All Available Jobs" />
</FrameLayout>
